/**
 * @interface
 * Represents a menu item in the header.
 */
export interface IHeaderMenuItem {
  /**
   * Name that is displayed in the menu on header.
   */
  name: string;
  /**
   * Url/Path of page it points to.
   */
  path: string;
}
